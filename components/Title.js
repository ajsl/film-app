import React from 'react';

import {TouchableHighlight, Text, StyleSheet} from 'react-native';

const Button = ({NamedNodeMap, onPress}) => {

	return (

		<TouchableHighlight onPress={ onPress } 
	)
}
